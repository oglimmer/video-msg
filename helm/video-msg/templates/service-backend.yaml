apiVersion: v1
kind: Service
metadata:
  name: {{ include "video-msg.backend.fullname" . }}
  labels:
    {{- include "video-msg.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "video-msg.backend.selectorLabels" . | nindent 4 }}
